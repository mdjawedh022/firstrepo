/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = {
  images: {
    // protocol: "https",
    domains: ["images-na.ssl-images-amazon.com"],
    // port: "",
    // pathname: "/[id]/**",
  },
};
