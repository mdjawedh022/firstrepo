

const initialState={
    book:[],
    isLoading:false,
    isError:false,

}
const reducer = (state,{type,payload}) => {
    switch(type){
        default:
        return state
    }
}

export default reducer
